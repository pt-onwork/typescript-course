const superHeros: string[] = [];
const superHeros2: number[] = [];
const heroPower : Array<number>=[]

type User={
  name: string
  isActive: boolean 
}

const allUsers: User[]=[]

allUsers.push({name:'parth', isActive: true })

const MLModels : number [][]= [
  [255,255,255],
]


superHeros.push('thor');
superHeros2.push(2)

export{}